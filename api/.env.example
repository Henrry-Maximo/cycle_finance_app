# Environment
NODE_ENV=dev
PORT=3333

# Auth (generate secret JWT)
JWT_SECRET=cyclefinance01

# Database
DATABASE_URL="postgresql://docker:docker@localhost:5432/apicyclefinance?schema=public"

# Frontend (exemplo for request reset password)
APP_URL=http://localhost:3333

# Redis (service docker cache)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=

# IA
GEMINI_API_KEY=
MAX_FILE_SIZE_MB=5
